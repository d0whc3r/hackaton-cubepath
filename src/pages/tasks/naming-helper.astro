---
import AppLayout from '@/layouts/AppLayout.astro'
import { TaskApp } from '@/components/chat/TaskApp'
---

<AppLayout fullScreen title="SLM Router — Naming Helper" description="Get rename suggestions for unclear variables and functions as a before → after list.">
  <TaskApp
    client:load
    fixedTaskType="naming-helper"
    pageTitle="Naming Helper"
    pageDescription="Get rename suggestions for unclear variables and functions as a before → after list."
  />
</AppLayout>

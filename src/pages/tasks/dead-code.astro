---
import AppLayout from '@/layouts/AppLayout.astro'
import { TaskApp } from '@/components/chat/TaskApp'
---

<AppLayout fullScreen title="SLM Router — Dead Code" description="Identify unused imports, unreachable code, and redundant variables in your file or fragment.">
  <TaskApp
    client:load
    fixedTaskType="dead-code"
    pageTitle="Dead Code"
    pageDescription="Identify unused imports, unreachable code, and redundant variables in your file or fragment."
  />
</AppLayout>
